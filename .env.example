# ===================================
# Environment Variables Template
# ===================================
#
# INSTRUCTIONS:
# 1. Copy this file to .env in the project root
# 2. Update the values with your configuration
# 3. The .env file is gitignored and won't be committed
#
# ===================================

# ===================================
# AIRFLOW CONFIGURATION
# ===================================

# Airflow UID (user ID for file permissions)
AIRFLOW_UID=50000

# Project directory (usually current directory)
AIRFLOW_PROJ_DIR=.

# Airflow image name (default: apache/airflow:3.1.2)
# AIRFLOW_IMAGE_NAME=apache/airflow:3.1.2

# Airflow admin credentials (default)
# _AIRFLOW_WWW_USER_USERNAME=airflow
# _AIRFLOW_WWW_USER_PASSWORD=airflow

# Airflow webserver port
# AIRFLOW_PORT=8080

# ===================================
# GOOGLE CLOUD CONFIGURATION
# ===================================

# Your GCP project ID (REQUIRED - update this!)
GCP_PROJECT_ID=your-gcp-project-id

# BigQuery dataset (optional - defaults set in profiles.yml)
# DBT_TARGET_DATASET=dev

# BigQuery location (optional)
# DBT_TARGET_LOCATION=US

# ===================================
# DATABASE CONFIGURATION (for Airflow metadata)
# ===================================

# PostgreSQL configuration
POSTGRES_USER=airflow
POSTGRES_PASSWORD=airflow
POSTGRES_DB=airflow

# ===================================
# OPTIONAL: ADDITIONAL CONFIGURATION
# ===================================

# Additional pip packages to install at runtime
# _PIP_ADDITIONAL_REQUIREMENTS=

# DBT variables (can override dbt_project.yml vars)
# DBT_BACKFILL=false
# DBT_START_DATE=2025-01-01
# DBT_END_DATE=2025-01-31

# ===================================
# NOTES
# ===================================
#
# Most settings have sensible defaults and don't need to be changed.
# The main thing you MUST update is GCP_PROJECT_ID.
#
# After creating .env file with your values:
# docker-compose up -d
#
# ===================================

